name: suji
description: 写下想说的话

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.1
  cupertino_icons: ^1.0.0

flutter:
  uses-material-design: true